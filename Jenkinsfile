pipeline {
    // agent { docker 'maven:3.3.3' }
    agent any
    stages {
        stage('build') {
            steps {
                // sh 'sudo mvn --version'
                sh 'echo $PATH'
            }
        }
}